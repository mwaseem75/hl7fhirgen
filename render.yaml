services:
  - type: web
    name: hl7fhirgen-playground
    runtime: docker
    dockerfilePath: ./webapp/Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: PORT
        value: 8000
