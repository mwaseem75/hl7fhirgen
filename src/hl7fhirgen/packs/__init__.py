"""Vertical-specific packs built on top of the generic hl7fhirgen engine.

A pack adds domain knowledge (e.g. a rejection-code knowledge base) on top of
the profile-agnostic generator/validator/explainer — it never changes how
those work generically.
"""
